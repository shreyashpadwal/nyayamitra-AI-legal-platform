--extra-index-url https://download.pytorch.org/whl/cpu
# Force CPU-only PyTorch build to avoid >1GB of unused CUDA downloads
torch

fastapi
uvicorn
python-dotenv

# AI / RAG
langchain
langchain-core
langchain-community
langchain-groq
langchain-text-splitters
faiss-cpu
sentence-transformers
pypdf

# LangGraph + Advanced RAG
langgraph
tavily-python
rank-bm25

# Evaluation deps (NOT installed in production Docker image)
# Install separately for local eval runs:  pip install -r requirements-dev.txt

# Core
pydantic

# Database
sqlalchemy
psycopg2-binary
pandas

# Security
passlib[bcrypt]
python-jose[cryptography]

# Document Export
fpdf2
python-docx